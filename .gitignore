# Secrets - never commit
.env

# Environment
.venv/
__pycache__/
*.pyc

# Vendored LangChain skill bundle (project scaffold, not app code)
agent/
skills-lock.json

# Saved embedding dumps (multi-MB JSON)
embeddings_app/out/

# FAISS index - a build artefact, regenerate with: rag.py --build
rag_app/faiss_index/
